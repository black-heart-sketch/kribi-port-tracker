doctype html
html
  head
    style(type='text/css')
      | body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
      | .header { background-color: #1a365d; color: white; padding: 20px; text-align: center; border-radius: 5px 5px 0 0; }
      | .content { padding: 20px; border: 1px solid #e2e8f0; border-top: none; border-radius: 0 0 5px 5px; }
      | .button { display: inline-block; background-color: #3182ce; color: white; padding: 10px 20px; text-decoration: none; border-radius: 4px; margin: 15px 0; }
      | .details { margin: 15px 0; padding: 10px; background: #f7fafc; border-left: 4px solid #3182ce; }
      | .footer { margin-top: 20px; font-size: 12px; color: #718096; text-align: center; }
  body
    .header
      h2 New Berthing Notification
    .content
      p Hello #{firstName},
      p A new berthing has been created and requires your attention.
      
      .details
        h3 Berthing Details:
        p Ship: #{berthing.ship.name}
        p IMO: #{berthing.ship.imoNumber}
        p Arrival: #{new Date(berthing.arrivalDate).toLocaleString()}
        p Departure: #{new Date(berthing.departureDate).toLocaleString()}
        p Dock: #{berthing.dock.name}
        p Status: #{berthing.status}
        
        if berthing.cargoDetails && berthing.cargoDetails.length > 0
          h4 Cargo Details:
          each cargo in berthing.cargoDetails
            p • #{cargo.description} - #{cargo.weight} tons
      
      a.button(href=url) View Berthing Details
      
      p If you have any questions, please contact the port authority.
      p Best regards,
      p The Kribi Port Authority Team
    .footer
      p &copy; #{new Date().getFullYear()} Kribi Port Authority. All rights reserved.
